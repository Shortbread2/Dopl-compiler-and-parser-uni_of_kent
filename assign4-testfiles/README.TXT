The valid folder contains input files that are all correct DOPL programs, so the output
should be ok for each.

The error folder contains input files that are all incorrect DOPL programs, so the output
should be error for each.

